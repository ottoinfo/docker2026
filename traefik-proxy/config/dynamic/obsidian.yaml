# http:
#   routers:
#     it-tools:
#       entryPoints:
#       - https
#       rule: "Host(`obsidian.${SERVER_NAME}.${TOP_LEVEL_DOMAIN}`)"
#       middlewares:
#       - secured
#       tls:
#         domains:
#         - main: "${SERVER_NAME}.${TOP_LEVEL_DOMAIN}"
#           sans:
#           - "*.${SERVER_NAME}.${TOP_LEVEL_DOMAIN}"
#       service: obsidian-docker@docker
